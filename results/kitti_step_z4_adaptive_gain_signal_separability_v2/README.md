# Adaptive Gain causal signal separability v2

The first-pass B_t labels are reused exactly. Frozen Host entropy, confidence, and margin are added; error-only, Host-only, and seven-signal logistic regression use sequence-held-out LOSO.
